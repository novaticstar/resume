// This file contains JavaScript code for interactive features of the resume website. 
// You can add functions for form validation, dynamic content updates, or other client-side behaviors here.

document.addEventListener('DOMContentLoaded', () => {
    // Example: Function to display a welcome message
    const welcomeMessage = () => {
        alert('Welcome to my resume website!');
    };

    welcomeMessage();
});